from setuptools import setup

setup(name='football_match_prediction',
      version='0.1',
      description='Build features datasetfrom provided data points, apply model for predicting outcomes',
      packages=[''],
      zip_safe=False)